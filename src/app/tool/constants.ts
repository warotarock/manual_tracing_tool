
export enum EditModeID {

  editMode = 1,
  drawMode
}

export enum OperationUnitID {

  none = 0,
  strokePoint,
  strokeSegment,
  stroke,
  layer,
  countOfID,
}

export enum OperationOriginTypeID {

  none = 0,
  medianCenter,
  pivot,
}
